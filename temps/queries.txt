USE wiscon106; SELECT ten, COUNT(ten) AS count FROM wiscondef106 GROUP BY wiscondef106.ten;
USE wiscon106; SELECT hundred, COUNT(hundred) AS count FROM wiscondef106 GROUP BY wiscondef106.hundred;
USE wiscon106; SELECT kgroups, COUNT(kgroups) AS count FROM wiscondef106 GROUP BY wiscondef106.kgroups;
USE wiscon106; SELECT tenkgroups, COUNT(tenkgroups) AS count FROM wiscondef106 GROUP BY wiscondef106.tenkgroups;
USE wiscon106; SELECT hundredkgroups, COUNT(hundredkgroups) AS count FROM wiscondef106 GROUP BY wiscondef106.hundredkgroups;
USE wiscon106; SELECT milliongroups, COUNT(milliongroups) AS count FROM wiscondef106 GROUP BY wiscondef106.milliongroups;
USE wiscon106; SELECT similarstring, COUNT(similarstring) AS count FROM wiscondef106 GROUP BY wiscondef106.similarstring;
USE wiscon106; SELECT normalstring, COUNT(normalstring) AS count FROM wiscondef106 GROUP BY wiscondef106.normalstring;
USE wiscon106; SELECT variablelengthstring, COUNT(variablelengthstring) AS count FROM wiscondef106 GROUP BY wiscondef106.variablelengthstring;
USE wiscon106; SELECT variablelengthstring,milliongroups, COUNT(*) AS count FROM wiscondef106 GROUP BY wiscondef106.variablelengthstring, wiscondef106.milliongroups;
USE wiscon106; SELECT similarstring,milliongroups, COUNT(*) AS count FROM wiscondef106 GROUP BY wiscondef106.similarstring, wiscondef106.milliongroups;
USE wiscon106; SELECT ten, SUM(ten) AS SUM FROM wiscondef106 GROUP BY wiscondef106.ten;
USE wiscon106; SELECT hundred, SUM(hundred) AS SUM FROM wiscondef106 GROUP BY wiscondef106.hundred;
USE wiscon106; SELECT kgroups, SUM(kgroups) AS SUM FROM wiscondef106 GROUP BY wiscondef106.kgroups;
USE wiscon106; SELECT tenkgroups, SUM(tenkgroups) AS SUM FROM wiscondef106 GROUP BY wiscondef106.tenkgroups;
USE wiscon106; SELECT hundredkgroups, SUM(hundredkgroups) AS SUM FROM wiscondef106 GROUP BY wiscondef106.hundredkgroups;
USE wiscon106; SELECT milliongroups, SUM(milliongroups) AS SUM FROM wiscondef106 GROUP BY wiscondef106.milliongroups;
USE wiscon106; SELECT variablelengthstring,milliongroups, SUM(milliongroups) AS SUM FROM wiscondef106 GROUP BY wiscondef106.variablelengthstring, wiscondef106.milliongroups;
USE wiscon106; SELECT similarstring,milliongroups, SUM(milliongroups) AS SUM FROM wiscondef106 GROUP BY wiscondef106.similarstring, wiscondef106.milliongroups;
USE wiscon107; SELECT ten, COUNT(ten) AS count FROM wiscondef107 GROUP BY wiscondef107.ten;
USE wiscon107; SELECT hundred, COUNT(hundred) AS count FROM wiscondef107 GROUP BY wiscondef107.hundred;
USE wiscon107; SELECT kgroups, COUNT(kgroups) AS count FROM wiscondef107 GROUP BY wiscondef107.kgroups;
USE wiscon107; SELECT tenkgroups, COUNT(tenkgroups) AS count FROM wiscondef107 GROUP BY wiscondef107.tenkgroups;
USE wiscon107; SELECT hundredkgroups, COUNT(hundredkgroups) AS count FROM wiscondef107 GROUP BY wiscondef107.hundredkgroups;
USE wiscon107; SELECT milliongroups, COUNT(milliongroups) AS count FROM wiscondef107 GROUP BY wiscondef107.milliongroups;
USE wiscon107; SELECT similarstring, COUNT(similarstring) AS count FROM wiscondef107 GROUP BY wiscondef107.similarstring;
USE wiscon106; SELECT normalstring, COUNT(normalstring) AS count FROM wiscondef107 GROUP BY wiscondef107.normalstring;
USE wiscon107; SELECT variablelengthstring, COUNT(variablelengthstring) AS count FROM wiscondef107 GROUP BY wiscondef107.variablelengthstring;
USE wiscon107; SELECT variablelengthstring,milliongroups, COUNT(*) AS count FROM wiscondef107 GROUP BY wiscondef107.variablelengthstring, wiscondef107.milliongroups;
USE wiscon107; SELECT similarstring,milliongroups, COUNT(*) AS count FROM wiscondef107 GROUP BY wiscondef107.similarstring, wiscondef107.milliongroups;
USE wiscon107; SELECT ten, SUM(ten) AS SUM FROM wiscondef107 GROUP BY wiscondef107.ten;
USE wiscon107; SELECT hundred, SUM(hundred) AS SUM FROM wiscondef107 GROUP BY wiscondef107.hundred;
USE wiscon107; SELECT kgroups, SUM(kgroups) AS SUM FROM wiscondef107 GROUP BY wiscondef107.kgroups;
USE wiscon107; SELECT tenkgroups, SUM(tenkgroups) AS SUM FROM wiscondef107 GROUP BY wiscondef107.tenkgroups;
USE wiscon107; SELECT hundredkgroups, SUM(hundredkgroups) AS SUM FROM wiscondef107 GROUP BY wiscondef107.hundredkgroups;
USE wiscon107; SELECT milliongroups, SUM(milliongroups) AS SUM FROM wiscondef107 GROUP BY wiscondef107.milliongroups;
USE wiscon107; SELECT variablelengthstring,milliongroups, SUM(milliongroups) AS SUM FROM wiscondef107 GROUP BY wiscondef107.variablelengthstring, wiscondef107.milliongroups;
USE wiscon107; SELECT similarstring,milliongroups, SUM(milliongroups) AS SUM FROM wiscondef107 GROUP BY wiscondef107.similarstring, wiscondef107.milliongroups;
USE wiscon108; SELECT ten, COUNT(ten) AS count FROM wiscondef108 GROUP BY wiscondef108.ten;
USE wiscon108; SELECT hundred, COUNT(hundred) AS count FROM wiscondef108 GROUP BY wiscondef108.hundred;
USE wiscon108; SELECT kgroups, COUNT(kgroups) AS count FROM wiscondef108 GROUP BY wiscondef108.kgroups;
USE wiscon108; SELECT tenkgroups, COUNT(tenkgroups) AS count FROM wiscondef108 GROUP BY wiscondef108.tenkgroups;
USE wiscon108; SELECT hundredkgroups, COUNT(hundredkgroups) AS count FROM wiscondef108 GROUP BY wiscondef108.hundredkgroups;
USE wiscon108; SELECT milliongroups, COUNT(milliongroups) AS count FROM wiscondef108 GROUP BY wiscondef108.milliongroups;
USE wiscon108; SELECT similarstring, COUNT(similarstring) AS count FROM wiscondef108 GROUP BY wiscondef108.similarstring;
USE wiscon106; SELECT normalstring, COUNT(normalstring) AS count FROM wiscondef108 GROUP BY wiscondef108.normalstring;
USE wiscon108; SELECT variablelengthstring, COUNT(variablelengthstring) AS count FROM wiscondef108 GROUP BY wiscondef108.variablelengthstring;
USE wiscon108; SELECT variablelengthstring,milliongroups, COUNT(*) AS count FROM wiscondef108 GROUP BY wiscondef108.variablelengthstring, wiscondef108.milliongroups;
USE wiscon108; SELECT similarstring,milliongroups, COUNT(*) AS count FROM wiscondef108 GROUP BY wiscondef108.similarstring, wiscondef108.milliongroups;
USE wiscon108; SELECT ten, SUM(ten) AS SUM FROM wiscondef108 GROUP BY wiscondef108.ten;
USE wiscon108; SELECT hundred, SUM(hundred) AS SUM FROM wiscondef108 GROUP BY wiscondef108.hundred;
USE wiscon108; SELECT kgroups, SUM(kgroups) AS SUM FROM wiscondef108 GROUP BY wiscondef108.kgroups;
USE wiscon108; SELECT tenkgroups, SUM(tenkgroups) AS SUM FROM wiscondef108 GROUP BY wiscondef108.tenkgroups;
USE wiscon108; SELECT hundredkgroups, SUM(hundredkgroups) AS SUM FROM wiscondef108 GROUP BY wiscondef108.hundredkgroups;
USE wiscon108; SELECT milliongroups, SUM(milliongroups) AS SUM FROM wiscondef108 GROUP BY wiscondef108.milliongroups;
USE wiscon108; SELECT variablelengthstring,milliongroups, SUM(milliongroups) AS SUM FROM wiscondef108 GROUP BY wiscondef108.variablelengthstring, wiscondef108.milliongroups;
USE wiscon108; SELECT similarstring,milliongroups, SUM(milliongroups) AS SUM FROM wiscondef108 GROUP BY wiscondef108.similarstring, wiscondef108.milliongroups;
